interface:
  display_name: "Dreaming Course: Setup Project"
  short_description: "Create and verify a protected working copy"
  default_prompt: "Use $dreaming-course-setup-project after dependency preflight to create a protected course-project copy."

policy:
  allow_implicit_invocation: false
